# 辅助瞄准系统设计文档

> 本文档描述**当前代码**的架构与机制设计。与代码矛盾时以代码为准，并同步修正本文档（见 README「文档维护约定」）。
> 历史版本见 [docs/archive/](docs/archive/)。

## 系统概述

基于采集卡 + KmBox Net + YOLO 姿态检测的 FPS 辅助瞄准系统。
采集卡捕获游戏画面 → YOLO 识别人体目标 → KmBox 控制鼠标移动/点击。

### 硬件链路

```
鼠标 → KmBox → 主机
         ↑
      软件控制（UDP）
```

- **KmBox Net** 接在鼠标和主机之间，可屏蔽物理按键/移动，也可通过软件命令直接控制虚拟鼠标输出
- **Mask（屏蔽）**：阻止物理鼠标的指定消息传递给主机，但 KmBox 仍能检测到物理按键状态
- **软件命令**（MouseMove/MouseLeft）：直接控制虚拟输出，不受 Mask 影响

---

## 模块划分（后来人导航图）

| 模块 | 文件 | 职责 |
|---|---|---|
| 主循环 | `Form1.cs` | 采集帧回调 + YOLO 主循环（**KmBox 指令发送的唯一源头**）、UI |
| 武器调度 | `Firing/WeaponDispatcher.cs` | 按准心状态/按键决定当前帧走哪个射击分支 |
| 真人模式瞬狙 | `Firing/QuickScopeController.cs` | 四态状态机（见下文），消费左键意图代发 |
| 步枪会话 | `Firing/RifleSessionController.cs` | 右键=开火周期：右键按下开火、释放停火、期间辅助瞄准 |
| 左键屏蔽 | `Firing/LeftMaskController.cs` | 狙击模式左键屏蔽 + 物理左键边沿意图捕获（代发信号源） |
| 开火动作 | `Firing/FireActions.cs` | SniperFire：分段移动（≤120/段）+ 细粒度 XY 屏蔽 + 点击时序 |
| 准心检测 | `YoloProcessing/ImageHelper.cs` | 中心 2×2 像素判定狙/步准心；YOLO 推理封装 |
| 目标选择 | `YoloProcessing/TargetSelector.cs` | 23 部位系统、部位优先级表、尸体过滤 |
| 硬件封装 | `KmBox/KmBoxNet.cs` | KmBox Net UDP 协议、物理键鼠监控（事件推送） |
| 测试窗体 | `Test*Form.cs` | 隔离诊断工具，经 `Program.cs` 编译宏启动（见 README「测试入口」） |

---

## 武器模式判定机制

### 即时准心方案

系统通过 **准心检测** 区分步枪模式和狙击模式，而非依赖用户手动切换。

```
每帧检测准心 → 当前帧准心状态直接参与模式判定
右键按下时 → 读当前帧是否步枪准心
  → 是 → 步枪模式行为（步枪会话）
  → 否 → 狙击模式行为（真人模式瞬狙 / 代发）
```

### 准心判定规则

由 `ImageHelper.ReadGameCrosshairInfo` 采样中心 2×2 区域得出，同帧内狙/步互斥：

- 准心像素：`R=255 && B=0`（游戏场景极少出现，命中即准心或命中特效）
- `G=0` → 狙击（纯红准心）；`G>0` → 步枪（黄色准心，且涵盖命中渐变红→橙→黄全段）
- `SnipeEnabled = hasSnipePixel`；`RifleEnabled = !hasSnipePixel && hasRiflePixel`

### 为什么不再需要去抖

早期准心判定用 redness/yellowness 阈值，**命中敌人时准心变色**会造成模式误判闪烁，因此先后叠加过两套去抖，现已全部移除：

| 代次 | 机制 | 移除原因 |
|------|------|---------|
| 第一代 | 右键事件用“最近 50ms 内见过步枪准心”时间窗 | 切狙后 50ms 内按右键被误判为步枪 → 右键被屏蔽，**狙击镜打不开** |
| 第二代 | `CrosshairStabilizer` 帧数去抖（狙升 1 帧 / 其余 3 帧） | 阈值不对称使狙/步稳定态重叠 2~3 帧，切狙后仍走步枪分支 → 左键未屏蔽，**首枪辅助不触发**；反向切步则左键残留屏蔽/误代发一枪 |

新判定（`R=255 && B=0` + G 通道分流）从源头消除闪烁、且两态天然互斥，因此每帧直接使用检测结果，**切枪 0 帧延迟**。实战已验证正常。
（ISSUE-017 曾规划独立的 `CrosshairStabilizer` 组件，随本方案落地而废弃，该文件不存在。）

### 游戏特性利用

- 步枪：准心始终可见（非开镜状态也有），切到步枪后几帧内准心就出现
- 狙击：只有开镜才有红色准心，不开镜时无准心
- 切枪动画期间：两种准心都不会出现，形成天然的"模式隔离窗口"

---

## 步枪会话模式

### 核心设计

用户通过 **右键按压时长** 控制点射/连发，更符合游戏直觉：

```
右键按下 → 软件 MouseLeft(true) 开火
右键保持 → 有目标时辅助瞄准（MouseMove），无目标时用户自控弹道
右键释放 → 软件 MouseLeft(false) 停火
```

### XY 屏蔽机制

**问题**：右键按下到 YOLO 推理完成有延迟，期间用户可能手动移动鼠标造成冲突。

**解决**：右键按下时立即屏蔽物理鼠标 XY 轴移动。

```
右键按下（事件回调，零延迟）
  → MaskMouseX(true) + MaskMouseY(true)
  → 物理鼠标移动被屏蔽，用户能感知"鼠标锁定"
  → 软件 MouseMove（自瞄）不受影响，走独立命令通道

右键释放
  → MaskMouseX(false) + MaskMouseY(false)
  → 物理鼠标恢复自由
```

**为什么只屏蔽 XY 不屏蔽按键**：MaskAll（包含按键屏蔽）会重建 HID 报告，导致软件设置的左键保持状态被清除。只屏蔽 XY 轴（0x20, 0x40 位）不影响按键位（0x01, 0x02），软件 MouseLeft 状态安全。

### 左键时长补偿（已移除）

早期实现过"右键释放时 Sleep 补偿左键按压时长差"，因连续点射级联延迟、阻塞监控线程等问题**已移除**：左键实际按压比右键短约一帧，体感无感。完整决策记录见 ISSUES.md ISSUE-012。

### 首帧射击顺序

**原则**：必须先瞄准，再开火。

```
首帧有目标 → MouseMove(瞄准) → MouseLeft(true)(开火)
首帧无目标 → MouseLeft(true)(盲射，用户自控弹道)
后续帧     → 仅辅助瞄准，左键已保持
```

**为什么不能提前开火**：如果在 YOLO 推理前就按下左键，第一发子弹会打在移动前的位置，自瞄的第一发必然打不到目标。

### 准心闪失保护

**问题**：射击时的枪口动画/后坐力会暂时干扰准心检测，`crosshair.RifleEnabled` 返回 false。如果直接结束会话释放左键，连发就变成了点射。

**解决**：会话活跃且右键仍按着时保持会话，会话只由右键释放事件结束（步枪会话分支在模式判定中优先于准心检测，见 ISSUE-013 修复 3）。

### 自瞄移动节流

**问题**：采集卡有 ~5ms 采集延迟，连续快速移动可能基于移动前的旧画面做决策；移动频率过高也可能被游戏反作弊检测。

**解决**：首次移动立即执行，后续移动按固定间隔节流（`RIFLE_MOVE_INTERVAL_MS`）。
已知缺陷：射击期间准心检测频繁失败会跳过整帧，实际移动频率低于设定值，见 ISSUE-011。

---

## 真人模式（瞬狙干预）

UI 开关「真人模式」（代码中沿用 QuickScope 命名）。核心语义：**用户按键 = 意图信号，所有实际开火动作由软件统一发出**。

### 左键意图捕获（LeftMaskController）

- 狙击模式（`!RifleEnabled`）下左键被全程屏蔽，物理左键不直接进游戏。屏蔽不可解除：解除后“右键按下再屏蔽”存在时间差，来不及拦截盲狙，且物理左键与软件代发会互相冲突
- 菜单态优化：无准心且无右键时的左键意图（典型为游戏菜单点击）走 `FireActions.QuickClick` 快速点击（随机 30~50ms 按压，无拉栓冷却），不把完整开枪节奏套到菜单操作
- KmBox 监听线程（事件推送）在硬件边沿调用 `OnHwLeftEdge(isDown)`，**只标记意图不发指令**
- YOLO 主循环通过 `ConsumeManualFireRequest()` 消费意图（边沿触发：按下一次发一枚，按住不连发）

### 防盲狙闸门（QuickScopeController）

```
Idle ──右键按下──▶ Waiting ──检测到开镜──▶ Released（交还 Form1 常规流程）
                    │
                    └──超过 WaitForScopeMs 未开镜────▶ Released（放行盲射意图）
```

| 状态 | 语义 |
|---|---|
| **Idle** | 空闲，等待右键事件 |
| **Waiting** | 右键按下后等待狙击准心出现（开镜动画期），最长 `WaitForScopeMs`（默认 70ms，代码常量）；期间独占本帧（不跑 YOLO），挂起的左键意图不消费 |
| **Released** | 闸门已通过（开镜成功或等待超时），状态机不再干预；全部开火决策由 Form1 常规流程处理 |

**闸门的语义**：只保证“未开镜不代发”（防盲狙）。开镜后的代发决策（有目标→自瞄代发 / 无目标→盲射准心，含微自瞄过滤与盲射帧数控制）统一由 Form1 `ProcessYoloFrame` step 7 真人模式分支执行。

**历史**：早期版本含 Monitoring 状态（开镜后由本类独占帧跑 YOLO、消费意图代发），与 Form1 主循环真人模式分支逐行等价，属重复实现，已连同“监控窗口”（WindowMs）一起删除；旧 UI「监控(ms)」输入框只在两套等价实现间切换（其旧版“调到 1 → 左键直通纯手动”的效果在左键屏蔽改为全程持有后已丢失），一并删除。

已知泄漏点（调查中）：开镜后 `snipeEnabled` 闪丢 1 帧，挂起意图会落入未开镜 fast-path 盲射放行。见 ISSUE-022。

### 右键预屏蔽（历史方案）

早期版本在右键按下时立即屏蔽左键等准心出现。现行实现中左键屏蔽由 `LeftMaskController` 按准心状态统一驱动（`!RifleEnabled` 即屏蔽），不再有独立预屏蔽阶段。

### 微自瞄

「微自瞄」开关（默认关闭）：准心在识别框外扩 `_microAimExtend` 像素（UI 默认 30）内才算“有效目标”走自瞄代发，框外按无目标盲射准心。过滤在 Form1 step 7 真人模式分支全局生效。
历史边界抖动双重触发问题已修复（ISSUE-015：消费锁死 + 新鲜边沿检测，一次开镜 ≤1 次代发）。

### 盲射帧数

「盲射帧数」输入框（0~10，默认 1，仅真人模式）：控制左键意图后自瞄介入的深度：
- 0 = 不执行自瞄：不看识别结果，左键意图只原地代发
- 1（默认）= 只看意图帧识别结果：有目标→瞄准代发，无→立即原地盲射
- N≥2 = 除意图帧外多等 N-1 帧，等待期间任意识别到目标转瞄准代发，否则原地盲射。应对“开镜后第一帧 YOLO 识别不到人”的场景

意图帧计为第 1 帧；开镜边沿（新会话）与 KmBox 断开时强制清零挂起，防残留开枪。

---

## KmBox Mask 机制详解

### Mask 标志位

| 位 | 含义 | 方法 |
|----|------|------|
| 0x01 | 屏蔽左键 | MaskMouseLeft |
| 0x02 | 屏蔽右键 | MaskMouseRight |
| 0x20 | 屏蔽X轴移动 | MaskMouseX |
| 0x40 | 屏蔽Y轴移动 | MaskMouseY |

### 屏蔽职责归属（现行）

| 屏蔽对象 | 持有者 | 触发条件 |
|---|---|---|
| 左键 | `LeftMaskController` | `!RifleEnabled`（狙击模式全程） |
| 右键 | `RifleSessionController` | 步枪准心可见或会话活跃（防游戏右+左冲突，ISSUE-013） |
| XY | `RifleSessionController` / `FireActions`（开火瞬间） | 步枪右键按下期间 / MouseMove 期间 |
| `MaskAll` | **禁用** | —（会重建 HID 报告清除软件按键状态） |
| `UnmaskAll` | 仅 Connect/Disconnect | 初始化/清理兜底 |

核心原则：**一键一主**（同一按键只有一个模块负责屏蔽）、**bool 与硬件 1:1 对齐**、**解除 = 对称操作**。完整规划背景见 ISSUE-016/017/020。

### 关键踩坑记录

1. **MaskAll 会清除软件按键状态**：KmBox 固件重建 HID 报告时会清除软件设置的左键保持状态。UnmaskAll 后恢复物理状态（用户没按左键→左键释放）。
   → **解决**：步枪会话中不使用 MaskAll，只使用 MaskMouseX/Y（不影响按键位）。
2. **MouseMove 的 button 参数问题**：原始 `MouseMove` 方法传 `button=0`，可能在某些固件版本中重置按键状态。已修复为使用 `_mouseButton`（当前软件按键状态）。
3. **Mask 命令是累积式的**：每次 MaskMouseX/Y/Left 只修改对应位，不影响其他位。多个 Mask 操作可以安全叠加。
4. **UnmaskAll 时硬件仍按下 = 凭空事件**：屏蔽解除瞬间硬件按键状态透传，游戏会看到凭空按下，是 CF「键鼠异常」弹窗的根因候选。见 ISSUE-014。

---

## UI 开关说明

（默认值以 `Form1.Designer.cs` 为准；`Form1_Load` 统一从控件读取初始值——Designer 的 `Checked = true` 不会触发 `CheckedChanged`。）

| 控件 | 默认值 | 作用 |
|------|--------|------|
| 真人模式 | 勾选 | 开启真人模式防盲狙闸门（三态 + 左键意图代发，开镜后走 step 7 统一代发） |
| 狙击切枪 | 未勾选 | 狙击开火后自动滚轮切枪 |
| 步枪打头 | 勾选 | 步枪模式锁定头部（取消勾选锁定身体）。可按**鼠标侧键1**快速切换，无需手动点 UI |
| 微自瞄 | 未勾选 | 准心在识别框外扩 N 像素内按框内处理（N 由旁边文本框控制，默认 30） |
| 盲射帧数 | 1 | 真人模式自瞄介入深度：0=不自瞄只原地代发，1=只看当前帧，N≥2 除当前帧外多等 N-1 帧 |

---

## 双模型架构

狙击和步枪使用不同 YOLO 模型，通过 UI 下拉框选择（启动时扫描 `./Models/*-pose.engine`）：

- **狙击模型**（默认 L）：精度高，推理慢，适合单发场景
- **步枪模型**（默认 M）：推理快，适合连发场景需要快速响应

模型在启动时加载，如果两个下拉框选择相同模型则共用实例，避免重复加载。
注意：engine 与构建显卡绑定，换卡需重建（实测 RTX5090→5060 推理抖动加剧，见 ISSUE-022）。
